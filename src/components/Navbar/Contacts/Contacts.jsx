import { NavLink } from 'react-router-dom';
import s from './Contacts.module.css'

const Contacts = (props) => {
    return (
        <div className={s.Contacts}>
            The content of Contacts
        </div>
    )
};

export default Contacts;